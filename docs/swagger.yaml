openapi: 3.0.0
info:
  title: Devices Continuous Behavioural Authentication (DCBA) Mechanisms API
  version: 1.0.0
  description: |
    API documentation for the DCBA Services.

    The **Authenticator Endpoints** are exclusively utilized by the Android application, 'Authenticator', which is deployed on employees' devices. 

    To interact with the **GET** endpoints of the **Service API Endpoints**, you must authorize by providing a JWT Authorization token.

    For testing purposes, you can use the following JWT token: **eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJ1c2VyX2lkIjoiZXhhbXBsZV91c2VyIiwicm9sZSI6InNlcnZpY2VfcHJvdmlkZXIifQ.ppLwt2OKzPSfgl1NwzJKhzU_rRykEv3ojm0VmLRoLwk**


    **Steps to authorize:**

    1. Click the **Authorize** button, at the top-right corner.
    2. Enter the token specified in the description.
    3. Click **Authorize** and close the dialog.
    4. You can now use the **Try it out** button on the Service API Endpoints.

    **Note:**  
    All **POST Service API Endpoints** require a valid `didRequester` parameter. To obtain a valid `didRequester`, first call the `GET /devices` endpoint to retrieve the list of available devices. Then, use one of the returned `did` identifiers as the `didRequester` value in your **POST Service API Endpoints** requests.



servers:
  - url: https://k8s-cluster.tango.rid-intrasoft.eu/development/dcba-backend/

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT



paths:
  # Service API Endpoints #

  # [1] GET /devices
  /devices:
    get:
      security:
        - bearerAuth: []
      tags:
        - Service API Endpoints  # Tag for service-facing endpoints
      summary: Fetch all devices
      description: Retrieves all device entries from the database. This route requires a valid JWT Bearer token in the Authorization header to access.
      responses:
        '200':
          description: Successfully fetched devices
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: "success"
                  message:
                    type: string
                    example: "Devices fetched successfully."
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        device_id:
                          type: string
                        did:
                          type: string
                        sub:
                          type: string
              examples:
                NoDeviceFetched:
                  summary: Example with no devices fetched
                  value:
                    status: "success"
                    message: "Devices fetched successfully."
                    data: []
                SingleDeviceFetched:
                  summary: Example list of a device
                  value:
                    status: "success"
                    message: "Devices fetched successfully."
                    data:
                      - device_id: "5c487cb4a62cb29d"
                        did: "urn:uuid:06e3f05a-51ce-46e9-a9b9-e2cf27fd10e7"
                        sub: "did:my:wallet"
                MultipleDevicesFetched:
                  summary: Example list of devices
                  value:
                    status: "success"
                    message: "Devices fetched successfully."
                    data:
                      - device_id: "5c487cb4a62cb29d"
                        did: "urn:uuid:06e3f05a-51ce-46e9-a9b9-e2cf27fd10e7"
                        sub: "did:my:wallet"
                      - device_id: "fc9a978d5223b680"
                        did: "urn:uuid:a886e2e6-63d8-4e5b-836a-345427eec5ff"
                        sub: "did:my:wallet"
                      - device_id: "gi9b459o5333c612"
                        did: "urn:uuid:b115d2e6-03c9-3t7h-000a-125374ffp9ll"
                        sub: "did:my:wallet"
        '401':
          description: Authorization token is missing or malformed
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                  message:
                    type: string
              examples:
                MissingAuthorizationToken:
                  summary: Missing Authorization header
                  value:
                    status: "failed"
                    message: "Authorization token is missing."
                MalformedAuthorizationToken:
                  summary: Malformed Authorization header
                  value:
                    status: "failed"
                    message: "Authorization token is malformed. It should start with 'Bearer '."
        '403':
          description: Unauthorized - Authorization token is invalid or expired
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                  message:
                    type: string
              examples:
                ExpiredToken:
                  summary: Authorization token has expired
                  value:
                    status: "failed"
                    message: "Authorization token has expired."
                InvalidToken:
                  summary: Authorization token is invalid
                  value:
                    status: "failed"
                    message: "Invalid Authorization token."
        '500':
          description: Internal server error while fetching devices
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: "failed"
                  message:
                    type: string
                    example: "Error fetching device data."




  # [2] GET /devices/online-shifts  
  /devices/online-shifts:
    get:
      tags:
        - Service API Endpoints  # Tag for service-facing endpoints
      security:
        - bearerAuth: []
      summary: Fetch online devices
      description: Retrieves devices that are currently online. This route requires a valid JWT Bearer token in the Authorization header to access.
      responses:
        '200':
          description: Successfully fetched online devices
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: "success"
                  message:
                    type: string
                    example: "Fetched online devices successfully."
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        device_id:
                          type: string
                        did:
                          type: string
                        sub:
                          type: string
              examples:
                NoOnlineDeviceFetched:
                  summary: Example with no online devices fetched
                  value:
                    status: "success"
                    message: "Fetched online devices successfully."
                    data: []
                SingleOnlineDeviceFetched:
                  summary: Example list of an online device
                  value:
                    status: "success"
                    message: "Fetched online devices successfully."
                    data:
                      - device_id: "5c487cb4a62cb29d"
                        did: "urn:uuid:06e3f05a-51ce-46e9-a9b9-e2cf27fd10e7"
                        sub: "did:my:wallet"
                MultipleOnlineDevicesFetched:
                  summary: Example list of online devices
                  value:
                    status: "success"
                    message: "Fetched online devices successfully."
                    data:
                      - device_id: "5c487cb4a62cb29d"
                        did: "urn:uuid:06e3f05a-51ce-46e9-a9b9-e2cf27fd10e7"
                        sub: "did:my:wallet"
                      - device_id: "fc9a978d5223b680"
                        did: "urn:uuid:a886e2e6-63d8-4e5b-836a-345427eec5ff"
                        sub: "did:my:wallet"
                      - device_id: "gi9b459o5333c612"
                        did: "urn:uuid:b115d2e6-03c9-3t7h-000a-125374ffp9ll"
                        sub: "did:my:wallet"
        '401':
          description: Authorization token is missing or malformed
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                  message:
                    type: string
              examples:
                MissingAuthorizationToken:
                  summary: Missing Authorization header
                  value:
                    status: "failed"
                    message: "Authorization token is missing."
                MalformedAuthorizationToken:
                  summary: Malformed Authorization header
                  value:
                    status: "failed"
                    message: "Authorization token is malformed. It should start with 'Bearer '."
        '403':
          description: Unauthorized - Authorization token is invalid or expired
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                  message:
                    type: string
              examples:
                ExpiredToken:
                  summary: Authorization token has expired
                  value:
                    status: "failed"
                    message: "Authorization token has expired."
                InvalidToken:
                  summary: Authorization token is invalid
                  value:
                    status: "failed"
                    message: "Invalid Authorization token."
        '500':
          description: Internal server error while fetching online devices
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: "failed"
                  message:
                    type: string
                    example: "Error fetching online devices."





  # [3] GET /devices/offline-shifts
  /devices/offline-shifts:
    get:
      tags:
        - Service API Endpoints
      security:
        - bearerAuth: []
      summary: Fetch offline devices
      description: Retrieves devices that are currently offline. This route requires a valid JWT Bearer token in the Authorization header to access.
      responses:
        '200':
          description: Successfully fetched offline devices
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: "success"
                  message:
                    type: string
                    example: "Fetched offline devices successfully."
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        device_id:
                          type: string
                        did:
                          type: string
                        sub:
                          type: string
              examples:
                NoOfflineDeviceFetched:
                  summary: Example with no offline devices fetched
                  value:
                    status: "success"
                    message: "Fetched offline devices successfully."
                    data: []
                SingleOfflineDeviceFetched:
                  summary: Example list of an offline device
                  value:
                    status: "success"
                    message: "Fetched offline devices successfully."
                    data:
                      - device_id: "5c487cb4a62cb29d"
                        did: "urn:uuid:06e3f05a-51ce-46e9-a9b9-e2cf27fd10e7"
                        sub: "did:my:wallet"
                MultipleOfflineDevicesFetched:
                  summary: Example list of offline devices
                  value:
                    status: "success"
                    message: "Fetched offline devices successfully."
                    data:
                      - device_id: "5c487cb4a62cb29d"
                        did: "urn:uuid:06e3f05a-51ce-46e9-a9b9-e2cf27fd10e7"
                        sub: "did:my:wallet"
                      - device_id: "fc9a978d5223b680"
                        did: "urn:uuid:a886e2e6-63d8-4e5b-836a-345427eec5ff"
                        sub: "did:my:wallet"
                      - device_id: "gi9b459o5333c612"
                        did: "urn:uuid:b115d2e6-03c9-3t7h-000a-125374ffp9ll"
                        sub: "did:my:wallet"
        '401':
          description: Authorization token is missing or malformed
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                  message:
                    type: string
              examples:
                MissingAuthorizationToken:
                  summary: Missing Authorization header
                  value:
                    status: "failed"
                    message: "Authorization token is missing."
                MalformedAuthorizationToken:
                  summary: Malformed Authorization header
                  value:
                    status: "failed"
                    message: "Authorization token is malformed. It should start with 'Bearer '."
        '403':
          description: Unauthorized - Authorization token is invalid or expired
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                  message:
                    type: string
              examples:
                ExpiredToken:
                  summary: Authorization token has expired
                  value:
                    status: "failed"
                    message: "Authorization token has expired."
                InvalidToken:
                  summary: Authorization token is invalid
                  value:
                    status: "failed"
                    message: "Invalid Authorization token."
        '500':
          description: Internal server error while fetching offline devices
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: "failed"
                  message:
                    type: string
                    example: "Error fetching offline devices."


  # [4] POST /devices/behavioural-score
  /devices/behavioural-score:
    post:
      tags:
        - Service API Endpoints
      security:
        - bearerAuth: []
      summary: Retrieve device behavioural score
      description: >
        Returns the behavioural score of a device based on provided DIDs.
        This route requires a valid JWT Bearer token in the Authorization header to access.
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              required:
                - didSP
                - didRequester
              properties:
                didSP:
                  type: string
                  description: The Decentralized Identifier (DID) of the service provider initiating the request.
                  example: "did:example:serviceprovider123"
                didRequester:
                  type: string
                  description: The Decentralized Identifier (DID) of the requesting employee.
                  example: "urn:uuid:2dcd4cb0-acb1-433f-b9a5-0d210f760618"

            examples:
              validRequest:
                summary: Valid request example
                value:
                  didSP: "did:example:serviceprovider123"
                  didRequester: "urn:uuid:2dcd4cb0-acb1-433f-b9a5-0d210f760618"
      responses:
        '200':
          description: Behavioural score retrieved successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: "success"
                  message:
                    type: string
                    example: "Device found."
                  behaviouralScore:
                    type: number
                    format: float
                    example: 0.85
        '400':
          description: Bad Request - Missing required fields
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: "failed"
                  message:
                    type: string
                    example: "Missing required fields: didSP or didRequester."
        '401':
          description: Unauthorized - Authorization token is invalid or expired
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: "failed"
                  message:
                    type: string
              examples:
                TokenExpired:
                  summary: Authorization token has expired
                  value:
                    status: "failed"
                    message: "Authorization token has expired."
                InvalidToken:
                  summary: Authorization token is invalid
                  value:
                    status: "failed"
                    message: "Invalid authorization token."
        '404':
          description: Device not found
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: "failed"
                  message:
                    type: string
                    example: "Device not found."
        '500':
          description: Internal server error while retrieving behavioural score 
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: "failed"
                  message:
                    type: string
                    example: "Error retrieving behavioural score."

  # [5] POST /devices/last-location
  /devices/last-location:
    post:
      tags:
        - Service API Endpoints
      security:
        - bearerAuth: []
      summary: Retrieve device last location
      description: >
        Returns the last location of a device
        based on provided employee DIDs.
        This route requires a valid JWT Bearer token in the Authorization header to access.
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              required:
                - didSP
                - didRequester
              properties:
                didSP:
                  type: string
                  description: The Decentralized Identifier (DID) of the service provider initiating the request.
                  example: "did:example:serviceprovider123"
                didRequester:
                  type: string
                  description: The Decentralized Identifier (DID) of the requesting employee.
                  example: "urn:uuid:2dcd4cb0-acb1-433f-b9a5-0d210f760618"
            examples:
              validRequest:
                summary: Valid request example
                value:
                  didSP: "did:example:serviceprovider123"
                  didRequester: "urn:uuid:2dcd4cb0-acb1-433f-b9a5-0d210f760618"
      responses:
        '200':
          description: Last known device location retrieved successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: "success"
                  message:
                    type: string
                    example: "Device found."
                  lastLocation:
                    type: string
                    example: "WAREHOUSE"
                  firstSeenAt:
                    type: string
                    format: date-time
                    example: "2025-05-29 11:17:50"
                  lastSeenAt:
                    type: string
                    format: date-time
                    example: "2025-05-29 11:18:50"
                  durationSeconds:
                    type: integer
                    example: 60


        '400':
          description: Bad Request - Missing required fields
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: "failed"
                  message:
                    type: string
                    example: "Missing required fields: didSP or didRequester."
        '401':
          description: Unauthorized - Authorization token is invalid or expired
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: "failed"
                  message:
                    type: string
                    example: "Authorization error message"
              examples:
                TokenExpired:
                  summary: Authorization token has expired
                  value:
                    status: "failed"
                    message: "Authorization token has expired."
                InvalidToken:
                  summary: Authorization token is invalid
                  value:
                    status: "failed"
                    message: "Invalid authorization token."

        '404':
          description: Device not found
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: "failed"
                  message:
                    type: string
                    example: "Device not found."
        '500':
          description: Internal server error while retrieving last location
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: "failed"
                  message:
                    type: string
                    example: "Error retrieving device last location."

  # [6] POST /devices/location-history
  /devices/location-history:
    post:
      tags:
        - Service API Endpoints
      security:
        - bearerAuth: []
      summary: Retrieve device location history within a specified timeframe
      description: >
        Returns the location history of a device based on provided employee DIDs.
        This route requires a valid JWT Bearer token in the Authorization header to access.
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              required:
                - didSP
                - didRequester
                - from
                - to
              properties:
                didSP:
                  type: string
                  description: The Decentralized Identifier (DID) of the service provider initiating the request.
                  example: "did:example:serviceprovider123"
                didRequester:
                  type: string
                  description: The Decentralized Identifier (DID) of the requesting employee.
                  example: "urn:uuid:2dcd4cb0-acb1-433f-b9a5-0d210f760618"
                from:
                  type: string
                  format: date-time
                  description: ISO8601 start date-time for filtering the location history.
                  example: "2025-05-28T12:00:00.003Z"
                to:
                  type: string
                  format: date-time
                  description: ISO8601 end date-time for filtering the location history.
                  example: "2025-05-28T12:15:00.083Z"
            examples:
              validRequest:
                summary: Valid request example
                value:
                  didSP: "did:example:serviceprovider123"
                  didRequester: "urn:uuid:2dcd4cb0-acb1-433f-b9a5-0d210f760618"
                  from: "2025-05-28T12:00:00.003Z"
                  to: "2025-05-28T12:15:00.083Z"
      responses:
        '200':
          description: Device location history retrieved successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: "success"
                  message:
                    type: string
                    example: "Device location history retrieved."
              examples:
                NoLocationHistoryFound:
                  summary: Example response with no location history
                  value:
                    status: "success"
                    message: "Device location history retrieved."
                    location_history: []
                singleLocation:
                  summary: Example response with single location entry
                  value:
                    status: "success"
                    message: "Device location history retrieved."
                    location_history:
                      - estimated_location: "WAREHOUSE"
                        firstSeenAt: "2025-05-29 11:19:26"
                        lastSeenAt: "2025-05-29 11:20:36"
                        durationSeconds: 70
                multipleLocations:
                  summary: Example response with multiple location entries
                  value:
                    status: "success"
                    message: "Device location history retrieved."
                    location_history:
                      - estimated_location: "WAREHOUSE"
                        firstSeenAt: "2025-05-29 11:19:26"
                        lastSeenAt: "2025-05-29 11:20:36"
                        durationSeconds: 70
                      - estimated_location: "PACKAGING_LINES"
                        firstSeenAt: "2025-05-29 11:20:36"
                        lastSeenAt: "2025-05-29 11:21:41"
                        durationSeconds: 65
                      - estimated_location: "PERMITTED_AREA"
                        firstSeenAt: "2025-05-29 11:21:41"
                        lastSeenAt: "2025-05-29 11:22:00"
                        durationSeconds: 19
        '400':
          description: Bad Request - Missing required fields or invalid timeframe
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: "failed"
                  message:
                    type: string
              examples:
                MissingFields:
                  summary: Missing required fields
                  value:
                    status: "failed"
                    message: "Missing required fields: didSP, didRequester, or from/to timestamps."
                InvalidTimestamps:
                  summary: Invalid timestamp format
                  value:
                    status: "failed"
                    message: "Invalid from/to format. Both must be valid ISO timestamps."
        '401':
          description: Unauthorized - Authorization token is missing, invalid, or expired
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: "failed"
                  message:
                    type: string
                    example: "Authorization token is missing or invalid."
              examples:
                TokenExpired:
                  summary: Authorization token has expired
                  value:
                    status: "failed"
                    message: "Authorization token has expired."
                InvalidToken:
                  summary: Authorization token is invalid
                  value:
                    status: "failed"
                    message: "Invalid Authorization token."
        '404':
          description: Device not found
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: "failed"
                  message:
                    type: string
                    example: "Device not found."
        '500':
          description: Internal server error while retrieving device location history
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: "failed"
                  message:
                    type: string
                    example: "Error retrieving device location history."



  # [7] POST /devices/permitted-location-history
  /devices/permitted-location-history:
    post:
      tags:
        - Service API Endpoints
      security:
        - bearerAuth: []
      summary: Retrieve device permitted location history within a specified timeframe
      description: >
        Returns the permitted location history of a device based on provided employee DIDs.
        This route requires a valid JWT Bearer token in the Authorization header to access.
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              required:
                - didSP
                - didRequester
                - from
                - to
              properties:
                didSP:
                  type: string
                  description: The Decentralized Identifier (DID) of the service provider initiating the request.
                  example: "did:example:serviceprovider123"
                didRequester:
                  type: string
                  description: The Decentralized Identifier (DID) of the requesting employee.
                  example: "urn:uuid:2dcd4cb0-acb1-433f-b9a5-0d210f760618"
                from:
                  type: string
                  format: date-time
                  description: ISO8601 start date-time for filtering the location history.
                  example: "2025-05-28T12:00:00.003Z"
                to:
                  type: string
                  format: date-time
                  description: ISO8601 end date-time for filtering the location history.
                  example: "2025-05-28T12:15:00.083Z"
            examples:
              validRequest:
                summary: Valid request example
                value:
                  didSP: "did:example:serviceprovider123"
                  didRequester: "urn:uuid:2dcd4cb0-acb1-433f-b9a5-0d210f760618"
                  from: "2025-05-28T12:00:00.003Z"
                  to: "2025-05-28T12:15:00.083Z"

                  
      responses:
        '200':
          description: Device permitted location history retrieved successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: "success"
                  message:
                    type: string
                    example: "Device permitted location history retrieved."
              examples:
                NoLocationHistoryFound:
                  summary: Example response with no permitted location entry
                  value:
                    status: "success"
                    message: "Device permitted location history retrieved."
                    location_history: []
                singleLocation:
                  summary: Example response with single permitted location entry
                  value:
                    status: "success"
                    message: "Device permitted location history retrieved."
                    permitted_location_history:
                      - estimated_location: "PERMITTED_AREA"
                        firstSeenAt: "2025-05-29 11:19:26"
                        lastSeenAt: "2025-05-29 11:20:36"
                        durationSeconds: 70
                multipleLocations:
                  summary: Example response with multiple permitted location entries
                  value:
                    status: "success"
                    message: "Device permitted location history retrieved."
                    permitted_location_history:
                      - estimated_location: "PERMITTED_AREA"
                        firstSeenAt: "2025-05-29 11:19:26"
                        lastSeenAt: "2025-05-29 11:20:36"
                        durationSeconds: 70
                      - estimated_location: "PERMITTED_AREA"
                        firstSeenAt: "2025-05-29 11:22:56"
                        lastSeenAt: "2025-05-29 11:23:56"
                        durationSeconds: 60
                      - estimated_location: "PERMITTED_AREA"
                        firstSeenAt: "2025-05-29 11:25:00"
                        lastSeenAt: "2025-05-29 11:27:05"
                        durationSeconds: 125
                

        '400':
          description: Bad Request - Missing required fields or invalid timeframe
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: "failed"
                  message:
                    type: string
              examples:
                MissingFields:
                  summary: Missing required fields
                  value:
                    status: "failed"
                    message: "Missing required fields: didSP, didRequester, or from/to timestamps."
                InvalidTimestamps:
                  summary: Invalid timestamp format
                  value:
                    status: "failed"
                    message: "Invalid from/to format. Both must be valid ISO timestamps."

        '401':
          description: Unauthorized - Authorization token is missing, invalid, or expired
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: "failed"
                  message:
                    type: string
                    example: "Authorization token is missing or invalid."
              examples:
                TokenExpired:
                  summary: Authorization token has expired
                  value:
                    status: "failed"
                    message: "Authorization token has expired."
                InvalidToken:
                  summary: Authorization token is invalid
                  value:
                    status: "failed"
                    message: "Invalid Authorization token."
        '404':
          description: Device not found
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: "failed"
                  message:
                    type: string
                    example: "Device not found."
        '500':
          description: Internal server error while retrieving device permitted location history
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: "failed"
                  message:
                    type: string
                    example: "Error retrieving device permitted location history."


  # [8] POST /devices/restricted-location-history
  /devices/restricted-location-history:
    post:
      tags:
        - Service API Endpoints
      security:
        - bearerAuth: []
      summary: Retrieve device restricted location history within a specified timeframe
      description: >
        Returns the restricted location history of a device based on provided employee DIDs.
        This route requires a valid JWT Bearer token in the Authorization header to access.
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              required:
                - didSP
                - didRequester
                - from
                - to
              properties:
                didSP:
                  type: string
                  description: The Decentralized Identifier (DID) of the service provider initiating the request.
                  example: "did:example:serviceprovider123"
                didRequester:
                  type: string
                  description: The Decentralized Identifier (DID) of the requesting employee.
                  example: "urn:uuid:2dcd4cb0-acb1-433f-b9a5-0d210f760618"
                from:
                  type: string
                  format: date-time
                  description: ISO8601 start date-time for filtering the location history.
                  example: "2025-05-28T12:00:00.003Z"
                to:
                  type: string
                  format: date-time
                  description: ISO8601 end date-time for filtering the location history.
                  example: "2025-05-28T12:15:00.083Z"
            examples:
              validRequest:
                summary: Valid request example
                value:
                  didSP: "did:example:serviceprovider123"
                  didRequester: "urn:uuid:2dcd4cb0-acb1-433f-b9a5-0d210f760618"
                  from: "2025-05-28T12:00:00.003Z"
                  to: "2025-05-28T12:15:00.083Z"
      responses:
        '200':
          description: Device restricted location history retrieved successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: "success"
                  message:
                    type: string
                    example: "Device restricted location history retrieved."
              examples:
                NoLocationHistoryFound:
                  summary: Example response with no restricted location entry
                  value:
                    status: "success"
                    message: "Device restricted location history retrieved."
                    location_history: []
                singleLocation:
                  summary: Example response with single restricted location entry
                  value:
                    status: "success"
                    message: "Device restricted location history retrieved."
                    restricted_location_history:
                      - estimated_location: "PACKAGING_LINES"
                        firstSeenAt: "2025-05-29 11:19:26"
                        lastSeenAt: "2025-05-29 11:20:36"
                        durationSeconds: 70
                multipleLocations:
                  summary: Example response with multiple restricted location entries
                  value:
                    status: "success"
                    message: "Device restricted location history retrieved."
                    restricted_location_history:
                      - estimated_location: "PACKAGING_LINES"
                        firstSeenAt: "2025-05-29 11:19:26"
                        lastSeenAt: "2025-05-29 11:20:36"
                        durationSeconds: 70
                      - estimated_location: "SORTING_LINES_4_AND_5"
                        firstSeenAt: "2025-05-29 11:20:36"
                        lastSeenAt: "2025-05-29 11:20:56"
                        durationSeconds: 20
                      - estimated_location: "WAREHOUSE"
                        firstSeenAt: "2025-05-29 11:22:00"
                        lastSeenAt: "2025-05-29 11:23:15"
                        durationSeconds: 75
        '400':
          description: Bad Request - Missing required fields or invalid timeframe
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: "failed"
                  message:
                    type: string
              examples:
                MissingFields:
                  summary: Missing required fields
                  value:
                    status: "failed"
                    message: "Missing required fields: didSP, didRequester, or from/to timestamps."
                InvalidTimestamps:
                  summary: Invalid timestamp format
                  value:
                    status: "failed"
                    message: "Invalid from/to format. Both must be valid ISO timestamps."

        '401':
          description: Unauthorized - Authorization token is missing, invalid, or expired
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: "failed"
                  message:
                    type: string
                    example: "Authorization token is missing or invalid."
              examples:
                TokenExpired:
                  summary: Authorization token has expired
                  value:
                    status: "failed"
                    message: "Authorization token has expired."
                InvalidToken:
                  summary: Authorization token is invalid
                  value:
                    status: "failed"
                    message: "Invalid Authorization token."
        '404':
          description: Device not found
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: "failed"
                  message:
                    type: string
                    example: "Device not found."
        '500':
          description: Internal server error while retrieving device restricted location history
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: "failed"
                  message:
                    type: string
                    example: "Error retrieving device restricted location history."

  # Service API Endpoints # 
















































  # Authenticator Endpoints #

  # [1] POST /authenticator/begin-session
  /authenticator/begin-session:
    post:
      summary: Handle device initiation process (QR SCANNER)
      description: Initiates a device session request by upserting into the database and generating a login QR. The result is reflected in the HTTP status and JSON body.
      tags:
        - Authenticator Endpoints
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              required:
                - device_id
                - qr_scanner_state_request
                - log_file_uri
              properties:
                device_id:
                  type: string
                  description: The ID of the device initiating the session.
                  example: 5c487cb4a62cb29d
                qr_scanner_state_request:
                  type: string
                  description: The state parameter for QR scanner.
                  example: a6f3fn529i2a0
                log_file_uri:
                  type: string
                  description: Log file URI, where offline logs will be appended for future transmission.
                  example: file:///data/user/0/com.dcbaauthenticator/files/46299a77c27b0d4c/authenticator_logs.txt
      responses:
        '200':
          description: Session request successfully created or updated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    enum: [success]
                  message:
                    type: string
                  deviceAuthQRCode:
                    type: string
                    description: The QR code (only present on success).
                  sessionRequest:
                    type: object
                    description: The session request object that was saved or updated.
              examples:
                Success:
                  summary: Successful session request
                  value:
                    status: success
                    message: QR Code generated successfully.
                    deviceAuthQRCode: "data:image/png;base64,..."
                    sessionRequest:
                      device_id: "5c487cb4a62cb29d"
                      qr_scanner_state_request: "a6f3fn529i2a0"
                      log_file_uri: "file:///data/user/0/com.dcbaauthenticator/files/46299a77c27b0d4c/authenticator_logs.txt"
                      timestamp: "2025-04-26T12:34:56.789Z"
        '400':
          description: Missing required input fields.
          content:
            application/json:
              examples:
                MissingDeviceID:
                  summary: Missing device_id
                  value:
                    status: failed
                    message: Device ID is missing, session request failed.
                MissingQrScannerState:
                  summary: Missing qr_scanner_state_request
                  value:
                    status: failed
                    message: QR scanner state request is missing, session request failed.
        '502':
          description: Error in verifier service response.
          content:
            application/json:
              examples:
                QRCodeNotFound:
                  summary: QR code not found in verifier response
                  value:
                    status: failed
                    message: QR code not found in the verifier service response.

        '500':
          description: Internal error during session request handling.
          content:
            application/json:
              examples:
                SessionHandlingError:
                  summary: Internal Server Error during session request
                  value:
                    status: failed
                    message: Error handling session request.
                QRCodeExtractionError:
                  summary: QR code extraction failure
                  value:
                    status: failed
                    message: Failed to extract QR code due to an internal server error.



  # [2] GET /authenticator/auth-callback
  /authenticator/auth-callback:
    get:
      summary: Handle the authentication callback from Tango Verifier
      description: >
        Exchanges an authorization code for a token, decodes the JWT, validates the device session request,
        and returns the appropriate status depending on session and device state.
      tags:
        - Authenticator Endpoints
      parameters:
        - name: code
          in: query
          required: true
          schema:
            type: string
          description: Authorization code returned by the Tango Verifier.
        - name: state
          in: query
          required: true
          schema:
            type: string
          description: Opaque state value to correlate the session request.
      responses:
        '200':
          description: Success — Device authenticated and marked online.
          content:
            application/json:
              schema:
                type: object
                required:
                  - status
                  - message
                  - decodedPayload
                properties:
                  status:
                    type: string
                    enum: [success]
                    example: success
                    description: Indicates the success of the operation.
                  message:
                    type: string
                    example: Authentication success. Device status updated to online.
                    description: A human-readable message explaining the result.
                  decodedPayload:
                    type: object
                    description: Decoded JWT payload containing claims such as sub, did, exp, etc.
                    example:
                      aud:
                        - "k8s-cluster.tango.rid-intrasoft.eu"
                      client_id: "did:web:ips.k8s-cluster.tango.rid-intrasoft.eu:did"
                      exp: 1747396583
                      iss: "did:web:ips.k8s-cluster.tango.rid-intrasoft.eu:did"
                      kid: "ULaW3a1G2l-m_Yh13uBGitxe4ULkzd5cAxh7tTgdeRs"
                      sub: "did:my:wallet"
                      verifiableCredential:
                        "@context":
                          - "https://www.w3.org/2018/credentials/v1"
                          - "https://example.com/custom-context"
                        credentialSchema:
                          id: "https://example.com/schema.json"
                          type: "JsonSchemaValidator2018"
                        credentialSubject:
                          id: "did:my:wallet"
                          # Add additional fields as relevant here
                        id: "urn:uuid:869f17ef-a48d-46d2-9567-322abea828a9"
                        issuanceDate: "2025-05-16T11:26:01Z"
                        issued: "2025-05-16T11:26:01Z"
                        issuer: "did:web:ips.k8s-cluster.tango.rid-intrasoft.eu:did"
                        proof:
                          type: "EcdsaSecp256k1VerificationKey2019"
                          # add other proof fields if desired
                        type:
                          - "VerifiableCredential"
                          - "IDCredential"
                        validFrom: "2025-05-16T11:26:01Z"


        '400':
          description: Missing required parameters.
          content:
            application/json:
              schema:
                type: object
                required:
                  - status
                  - message
                properties:
                  status:
                    type: string
                    enum: [failed]
                    example: failed
                    description: Indicates the operation failed due to client error.
                  message:
                    type: string
                    example: "Missing required parameters: code or state."
                    description: Explanation of what parameters are missing.

        '401':
          description: Unauthorized — Authentication failed due to missing or invalid credentials.
          content:
            application/json:
              schema:
                type: object
                required:
                  - status
                  - message
                properties:
                  status:
                    type: string
                    enum: [failed]
                    example: failed
                    description: Indicates that authentication was unsuccessful.
                  message:
                    type: string
                    description: Specific reason why the request was unauthorized.
              examples:
                tokenNotReceived:
                  summary: Missing authentication token
                  value:
                    status: failed
                    message: Authentication token not received.
                invalidPayload:
                  summary: Invalid token payload
                  value:
                    status: failed
                    message: Invalid authentication token payload.
                credentialsMismatch:
                  summary: Credentials don't match the device
                  value:
                    status: failed
                    message: Credentials don't match this device.


        '409':
          description: Conflict — Device is already online.
          content:
            application/json:
              schema:
                type: object
                required:
                  - status
                  - message
                properties:
                  status:
                    type: string
                    enum: [failed]
                    example: failed
                    description: Indicates the request could not be completed due to a conflict.
                  message:
                    type: string
                    enum: [Device already online.]
                    example: Device already online.
                    description: Conflict reason indicating the device is already marked as online.

        '410':
          description: Gone — Session request has expired.
          content:
            application/json:
              schema:
                type: object
                required:
                  - status
                  - message
                properties:
                  status:
                    type: string
                    enum: [failed]
                    example: failed
                    description: Indicates the resource is no longer available.
                  message:
                    type: string
                    enum: [Session request expired.]
                    example: Session request expired.
                    description: The session request has expired and is no longer valid.

        '500':
          description: Internal Server Error.
          content:
            application/json:
              schema:
                type: object
                required:
                  - status
                  - message
                properties:
                  status:
                    type: string
                    enum: [failed]
                    example: failed
                    description: Indicates an internal server error occurred.
                  message:
                    type: string
                    example: Internal server error. Authentication failed.
                    description: Error message describing the server failure.



  # [3] POST /authenticator/post-logs
  /authenticator/post-logs:
    post:
      summary: Accepts Android Logs from devices for risk assessment or localization
      description: |
        This endpoint accepts `logs` from Android devices along with a session `authToken` and device identifier (`did`) for authentication and validation.
        It performs multiple checks, including validation of log format, session token validity, expiration, and DID match.
        Upon successful validation, the log is queued for processing.
        Always returns HTTP 200 status regardless of the operation result, to maintain client-side stability. Check the `status` field in the response body for operation result.
      tags:
        - Authenticator Endpoints
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              required:
                - did
                - log
                - authToken
              properties:
                did:
                  type: string
                  description: The Decentralized Identifier (DID) of the Android device.
                  example: "did:example:12345"
                log:
                  type: string
                  description: The log data captured from the device.
                  example: "11-22 13:11:21.537   799  1004 I WifiService: setWifiEnabled package=com.android.settings uid=1000 enable=true"
                authToken:
                  type: string
                  description: The authentication token associated with the device session.
                  example: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
      responses:
        '200':
          description: A successful response containing the status of log processing. Always returns 200 regardless of the operation result, to maintain client-side stability. Check the `status` field in the response body.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: The status of the log capture operation.
                    example: "success"
                  message:
                    type: string
                    description: A human-readable message providing more context.
                    example: "Log successfully captured and queued for processing."
              examples:
                Success:
                  summary: Successful processing of logs
                  value:
                    status: success
                    message: Logs stored, analyzed and processed successfully.
                MissingLogData:
                  summary: Log data missing
                  value:
                    status: failed
                    message: Log data cannot be empty.
                MalformedLogDetected:
                  summary: Malformed log detected
                  value:
                    status: failed
                    message: Malformed log detected.
                MissingAccessToken:
                  summary: Authentication token missing
                  value:
                    status: failed
                    message: Authentication token is required.
                InvalidAccessTokenFormat:
                  summary: Invalid authentication token format
                  value:
                    status: failed
                    message: Invalid authentication token format.
                FailedToDecodeAccessToken:
                  summary: Failed to decode authentication token
                  value:
                    status: failed
                    message: Failed to decode authentication token.
                AuthTokenExpired:
                  summary: Authentication token expired
                  value:
                    status: failed
                    message: Authentication token has expired.
                AuthTokenDidMismatch:
                  summary: Authentication token DID mismatch
                  value:
                    status: failed
                    message: Authentication token did does not match the provided did.
                MissingTimestampInLog:
                  summary: Missing timestamp in log data
                  value:
                    status: failed
                    message: Invalid log data. Missing timestamp.
                FailedToStoreLogs:
                  summary: Failed to store logs
                  value:
                    status: failed
                    message: Failed to store logs in the Influx Database.
                FailedToPerformLocalization:
                  summary: Localization execution failed
                  value:
                    status: failed
                    message: Failed to perform localization.
                FailedToPerformRiskAssessmentEngine:
                  summary: Risk assessment execution failed
                  value:
                    status: failed
                    message: Failed to perform risk assessment.
                InternalServerError:
                  summary: Internal server error
                  value:
                    status: failed
                    message: Internal server error while processing session request.

  # [4] GET /authenticator/auth-token-validation
  /authenticator/auth-token-validation:
    get:
      summary: Validate the provided Bearer JWT authentication token to ensure the employee remains logged into the Authenticator Android Application
      description: |
        This endpoint validates the provided Bearer JWT token and checks if it is expired or malformed.
        Returns appropriate HTTP status codes based on validation results.
      tags:
        - Authenticator Endpoints
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              required:
                - authToken
                - deviceID
              properties:
                authToken:
                  type: string
                  description: The authentication token associated with the device session.
                  example: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
                deviceID:
                  type: string
                  description: Device ID associated with the token.
                  example: 5c487cb4a62cb29d

      responses:
        '200':
          description: Valid token, device found.
          content:
            application/json:
              schema:
                type: object
                required:
                  - status
                  - message
                  - did
                  - sub
                  - verifiableCredential
                properties:
                  status:
                    type: string
                    enum: [success]
                    example: success
                    description: Indicates successful validation.
                  message:
                    type: string
                    example: valid
                    description: Human-readable status message.
                  did:
                    type: string
                    example: did:web:ips.k8s-cluster.tango.rid-intrasoft.eu:did
                    description: The DID (Decentralized Identifier) associated with the token.
                  sub:
                    type: string
                    example: did:my:wallet
                    description: Subject claim from the token.
                  verifiableCredential:
                    type: object
                    description: The verifiable credential from the token payload.
                    example:
                      id: urn:uuid:869f17ef-a48d-46d2-9567-322abea828a9
        '400':
          description: Bad request - malformed or invalid token.
          content:
            application/json:
              schema:
                type: object
                required:
                  - status
                  - message
                properties:
                  status:
                    type: string
                    enum: [failed]
                    example: failed
                  message:
                    type: string
                    description: Explanation of the error.
              examples:
                MalformedToken:
                  summary: Malformed Authorization Token
                  value:
                    status: failed
                    message: "Authentication token is malformed. It should start with 'Bearer '."
                InvalidFormat:
                  summary: Token format is invalid or cannot be decoded
                  value:
                    status: failed
                    message: "Invalid authentication token format."
                MissingDID:
                  summary: Device ID missing from token payload
                  value:
                    status: failed
                    message: "Device ID missing from authentication token."

        '401':
          description: Unauthorized - missing or expired token.
          content:
            application/json:
              schema:
                type: object
                required:
                  - status
                  - message
                properties:
                  status:
                    type: string
                    enum: [failed]
                    example: failed
                  message:
                    type: string
                    description: Explanation of why the authentication failed.
              examples:
                MissingToken:
                  summary: Authentication token is missing
                  value:
                    status: failed
                    message: "Authentication token is missing."
                ExpiredToken:
                  summary: Authentication token has expired
                  value:
                    status: failed
                    message: "Authentication token has expired."

        '404':
          description: Device associated with DID not found.
          content:
            application/json:
              schema:
                type: object
                required:
                  - status
                  - message
                properties:
                  status:
                    type: string
                    enum: [failed]
                    example: failed
                  message:
                    type: string
                    example: "Device not found."
        '500':
          description: Internal server error during token validation.
          content:
            application/json:
              schema:
                type: object
                required:
                  - status
                  - message
                properties:
                  status:
                    type: string
                    enum: [failed]
                    example: failed
                  message:
                    type: string
                    example: "Internal server error while validating authentication token."



  # [5] POST /authenticator/logout
  /authenticator/logout:
    post:
      summary: Logs out a device by marking its status as "offline" in the database
      description: |
        This endpoint allows a device to log out by providing an `authToken`, `did`, and `deviceID`. The request validates the authentication token and identifiers. If the device is found, its status is updated to "offline". The operation returns appropriate HTTP status codes with a JSON body indicating success or failure.
      tags:
        - Authenticator Endpoints
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              required:
                - authToken
                - did
                - deviceID
              properties:
                authToken:
                  type: string
                  description: The authentication token associated with the device session.
                  example: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
                did:
                  type: string
                  description: The Decentralized Identifier (DID) of the Android device.
                  example: "did:example:12345"
                deviceID:
                  type: string
                  description: Device ID associated with the Auth Token.
                  example: "5c487cb4a62cb29d"
      responses:
        '200':
          description: Device logged out successfully.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: success
                  message:
                    type: string
                    example: Device logged out successfully.
              examples:
                Success:
                  summary: Successful logout
                  value:
                    status: success
                    message: Device logged out successfully.
        '400':
          description: Bad request due to missing required fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: failed
                  message:
                    type: string
              examples:
                MissingAuthToken:
                  summary: Missing Authentication token
                  value:
                    status: failed
                    message: Missing required authentication token.
                MissingDid:
                  summary: Missing DID
                  value:
                    status: failed
                    message: Missing required did.
                MissingDeviceID:
                  summary: Missing device ID
                  value:
                    status: failed
                    message: Missing required device ID.
        '404':
          description: Device not found in the database.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: failed
                  message:
                    type: string
                    example: Device not found.
              examples:
                DeviceNotFound:
                  summary: Device not found
                  value:
                    status: failed
                    message: Device not found.
        '500':
          description: Internal server error during logout.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: failed
                  message:
                    type: string
                    example: Internal server error while handling logout.
              examples:
                GeneralInternalServerError:
                  summary: Internal Server Error
                  value:
                    status: failed
                    message: Internal server error while handling logout.
                InternalServerErrorDuringAuthTokenValidation:
                  summary: Internal Server Error
                  value:
                    status: failed
                    message: Internal server error while validating authentication token.
  # Authenticator Endpoints #


  # Server API Endpoints # 
  /server/status:
    get:
      summary: Check server status
      description: |
        This endpoint returns a simple success response indicating that the DCBA-backend server is up and running.
      tags:
        - Server API Endpoints
      responses:
        '200':
          description: Server is healthy and responding.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: success
                    description: Indicates the result of the server status check.
                  message:
                    type: string
                    example: "DCBA-backend server is up and functional."
                    description: A human-readable message confirming server functionality.
              examples:
                Healthy:
                  summary: Server is operational
                  value:
                    status: success
                    message: "DCBA-backend server is up and functional."
